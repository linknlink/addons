# Mosquitto 配置
# DeviceHub addon 内部 MQTT 代理

# 监听端口
listener 1883

# 允许匿名连接（容器内部使用）
allow_anonymous true

# 数据持久化
persistence true
persistence_location /var/lib/mosquitto/

# 日志输出到 stderr（由 supervisord 管理）
log_dest stderr
log_type error
log_type warning
log_type notice
